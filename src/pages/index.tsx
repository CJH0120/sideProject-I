import styles from '@/styles/Home.module.scss'
import className from 'classnames/bind'
import Link from 'next/link'

const cx = className.bind(styles)

export default function Home() {
   return <div>사이트 프로젝트 서버만 배포</div>
}
